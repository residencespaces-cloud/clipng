# ClipNG

Nigerian clipping marketplace UI — Next.js App Router + Tailwind.

## Running the code

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

```bash
npm run build   # production build
npm start       # serve production build
```
